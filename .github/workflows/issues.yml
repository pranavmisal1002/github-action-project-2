name: Handle issues
on:
  issues:
    types: [opened, edited, deleted, closed, reopened]
jobs:
    output-info:
        runs-on: ubuntu-latest
        steps:
          - name: Get code
            uses: actions/checkout@v4
          - name: Output issue info
            run: |
              echo "Issue "${{ toJson(github.event) }}""